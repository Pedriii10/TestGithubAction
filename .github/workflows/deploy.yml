name: Deploy

on:
  workflow_dispatch:

  push:
    branches: ["main"]

  #schedule:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: View downloaded repo
        run: ls -l
      - name: Install dependencies
        run: npm ci